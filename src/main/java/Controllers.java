import com.studiohartman.jamepad.ControllerManager;

public class Controllers extends  ControllerManager{
    public void init(){
        this.initSDLGamepad();
    }

        // while(true) System.out.println(controllers.getState(0).back);
        // b = A
        // dpad = dpad
        // start = START
        // y = B
        // back = SELECT
}
